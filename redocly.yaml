# See https://docs.redoc.ly/cli/configuration/ for more information.
organization: MediaEase
extends:
  - recommended
apis:
  main:
    root: ./docs/mediaease/components/api/openapi.yaml
rules:
  no-empty-servers: off
  info-license: off
  operation-4xx-response: off
  no-invalid-media-type-examples: off
theme:
  openapi:
    htmlTemplate: ./docs/mediaease/components/api/index.html
    theme:
      colors:
        primary:
          main: "#32329f"
